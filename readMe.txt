This program finds the Minimum Spanning Tree. You can either use hard-coded values or can enter your own input values. MST will be printed as a adjacency matrix.

To run this code you can either import the MST folder in Eclipse IDE and run it or you can compile and run MSTMain.java file.

